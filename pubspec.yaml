name: video_filter
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_ffmpeg: ^0.2.10
  video_player: ^0.10.11+1
  camera: 0.5.8+2
  path_provider: ^0.5.0
  flutter_spinkit: ^4.1.2+1
  cupertino_icons: ^0.1.3
  flutter_simple_sticker_view: ^1.0.0
  permission_handler: ^5.0.1+1
  image_gallery_saver: ^1.6.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/video/input.mp4
